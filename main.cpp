#include"Collection.h"

using namespace spa2;

int main(int argc, char* argv[]) {
    Map graph;
    graph.get_field(argv[1]);
    //solve_both(graph); // visualisating report
    //solve_with_Theta(graph); // usual report
    return 0;
}
